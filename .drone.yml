clone:
  git:
    image: plugins/git
    depth: 5

pipeline:
  phpunit:
    image: php:7.4
    commands:
      - composer install -q --prefer-dist
  nodejs:
    image: node
    commands:
      - npm install
      - npm run test
